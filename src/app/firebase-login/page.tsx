// This page is no longer used and can be deleted.
// The new login page is at /app-login and uses a simple password, not Firebase Auth.
export default function DeprecatedFirebaseLoginPage() {
  return null;
}
